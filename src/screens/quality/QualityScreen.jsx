// Phase-4 stub. A later phase replaces this file with the real Quality screen;
// nothing outside src/screens/quality/ needs to change when it does.

export default function QualityScreen() {
  return (
    <div>
      <h1 className="t-h1">Quality</h1>
      <p className="t-small" style={{ color: 'var(--ink-muted)' }}>
        Quality screen — coming in a later phase.
      </p>
    </div>
  );
}
