// Phase-4 stub. A later phase replaces this file with the real Approvals screen;
// nothing outside src/screens/approvals/ needs to change when it does.

export default function ApprovalsScreen() {
  return (
    <div>
      <h1 className="t-h1">Approvals</h1>
      <p className="t-small" style={{ color: 'var(--ink-muted)' }}>
        Approvals screen — coming in a later phase.
      </p>
    </div>
  );
}
