// Phase-4 stub. A later phase replaces this file with the real Agent screen;
// nothing outside src/screens/agent/ needs to change when it does.

export default function AgentScreen() {
  return (
    <div>
      <h1 className="t-h1">Agent</h1>
      <p className="t-small" style={{ color: 'var(--ink-muted)' }}>
        Agent screen — coming in a later phase.
      </p>
    </div>
  );
}
