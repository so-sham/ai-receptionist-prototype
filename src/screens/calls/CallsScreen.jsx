// Phase-4 stub. A later phase replaces this file with the real Calls screen;
// nothing outside src/screens/calls/ needs to change when it does.

export default function CallsScreen() {
  return (
    <div>
      <h1 className="t-h1">Calls</h1>
      <p className="t-small" style={{ color: 'var(--ink-muted)' }}>
        Calls screen — coming in a later phase.
      </p>
    </div>
  );
}
