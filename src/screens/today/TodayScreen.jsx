// Phase-4 stub. A later phase replaces this file with the real Today screen;
// nothing outside src/screens/today/ needs to change when it does.

export default function TodayScreen() {
  return (
    <div>
      <h1 className="t-h1">Today</h1>
      <p className="t-small" style={{ color: 'var(--ink-muted)' }}>
        Today screen — coming in a later phase.
      </p>
    </div>
  );
}
